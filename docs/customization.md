# Customization

Use the options flow to point the integration at your helper entities and tune fallback text, notifications, and timer presets.

The example dashboards may need small edits to match the exact entity IDs Home Assistant creates in your instance.
